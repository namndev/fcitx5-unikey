Unikey (Vietnamese Input Method) engine support for Fcitx

Released under mixed GPLv2+

pre

$  sudo apt-get install extra-cmake-modules fcitx5-modules-dev libfcitx5config-dev libfcitx5core-dev libfcitx5utils-dev

Install:
$ mkdir build && cd build
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ..
$ make
# make install
